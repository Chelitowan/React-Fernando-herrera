const activo = true;

// let mensaje = '';

// if (!activo) {
//     mensaje = 'Activo';
// } else {
//     mensaje = 'Inactivo';
// }

// operador ternario
// const mensaje = (activo) ? 'Activo' : 'Inactivo';
// const mensaje = (activo) ? 'activo' : 'inactivo';

const mensaje = activo && 'Activo';
console.log(mensaje);
